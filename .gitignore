# Dependencies
node_modules/

# Build output
out/
dist/
release/

# Generated assets — reproducible via `npm run assets` (ADR-006 §2)
assets/dist/

# Test output
coverage/
playwright-report/
test-results/
.vitest/

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local

# Scratch space `tests/boundaries.test.ts` writes and deletes while it runs.
# Untracked but not ignored, a concurrent `git add -A` staged these mid-run and
# the commit hook then linted deliberately-illegal fixtures (phase-30).
src/sim/__boundary_fixtures__/
